КОМАНДЫ ПРЕПРОЦЕССОРА
  #include <...> - подключение стандартной библиотеки (поиск производится в каталоге /usl/include)
  #include "..." - подключение пользовательской библиотеки (поиск производится в текущем каталоге)
  #define имя значение - объявление константы препроцессора (во всех вхождениях константы в коде она будет заменена на значение)
  #define max(a, b) (a > b)? a : b - параметризованный макрос
  #undef имя - удаление константы препроцессора
  #if выражение - условная компиляция
  #elif выражение - иначе если
  #else - иначе
  #endif - завершение конструкции if
  #ifdef имя - исполнение тела если константа существует
  #ifndef имя - исполнение тела если константа не существует
