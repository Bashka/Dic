ИНИЦИАЛИЗАЦИЯ
  struct имя{переменные}; - объявление структуры
  struct имя{переменные} переменные; - объявление структуры и переменных этой структуры
  struct имя переменные; - объявление переменных структуры
  struct имя переменная = {значение, ...} - объявление и инициализация переменных структуры
  typedef struct имя{...} синоним; - объявление структуры и определение ее типового синонима
  struct имя{struct имя{...} переменная; ...}; - вложенные структуры

ДОСТУП
  переменная.переменная - получение доступ к конкретному компоненту структуры
