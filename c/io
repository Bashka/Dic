НИЗКОУРОВНЕВЫЙ I/O
  open - открытие файла с правами доступа
  close - закрывает файл
  read - считывает строку из файла
  writer - записывает в файл
  creat - создает файл
  rename - переименовывает файл
  lseek - смещение внутреннего указателя
  EOF - признак конца потока (обычно -1)

СТАНДАРТНЫЕ ПОТОКИ
  0 (STDIN) - целочисленный идентификатор входного потока
  1 (STDOUT) - целочисленный идентификатор выходного потока
  2 (STDERR) - целочисленный идентификатор потока ошибок
  const FILE* stdin - указатель на входной поток
  const FILE* stdout - указатель на выходной поток
  const FILE* stderr - указатель на выходной поток ошибок
  getchar - чтение очередного символа из входного потока
  scanf - чтение форматированной строки из входного потока
  putchar - запись очередного символа в выходной поток
  printf - запись форматированной строки в выходной поток

ФОРМАТИРОВАНИЕ
  %s - строка
  %c - символ
  %i - целое число
  %f - дробное число
  %o - восьмеричное целое
  %x - шестнадцатиричное целое
  %5s - строка в блоке из 5 символов (пробелов)
  %.5s - строка из не более чем 5 символов (блок сужается до размеров строки)
  %.2f - дробное число с 2 знаками после запятой
  %-5s - строка с выравниванием по левому краю (по умолчанию выравнивание производится по правому краю)

ВЫСОКОУРОВНЕВЫЙ I/O
  fopen - открыть файл
  fclose - закрыть файл
  fileno - возвращает целочисленный дескриптор потока
  fseek - смещение указателя
  getc - чтение очередного символа из файла
  fgets - чтение строки из файла
  fscanf - аналог scanf работающий с файловыми дескрипторами
  ungetc - возвращает символ в поток
  putc - запись символа в файл
  fputs - запись строки в файл
  fprintf - аналог printf работающий с файловыми дескрипторами
  feof - возвращает ненулевое значение если достигнут конец файла
