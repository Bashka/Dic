FOR
  for имя in массив endfor - последовательное присвоение очередного значения из массива переменной
  for [a,b] in массив endfor - последовательное присвоение очередного значения из многомерного списка переменной

WHILE
  while условие тело endwhile - структура while
