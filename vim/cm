ВЫВОД ИНФОРМАЦИИ
  echo - вывод параметров в status bar 

ИСПОЛНЕНИЕ
  source файл - выполнение файла с vim скриптом
  source % - выполнение текущего файла

ПОЗИЦИОНИРОВАНИЕ КУРСОРА
  line(expr) - получить номер указанной строки
  getpos(expr) - получить информацию о запрашиваемой позиции в виде списка: [bnum, lnum, col, off]
  col(expr) - получить номер столбца
  cursor(lnum,col,[off]) - установить курсор на указанную позицию

ВОЗВРАТ
  getline(lnum,[end]) - возвращает указаные строки

ВСТАВКА
  setline(lnum,string) - устанавливает в качестве указанной строки значение
  append(lnum,string) - вставляет string в строку следующую за lnum
