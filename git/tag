МЕТКИ
  git tag <имя метки> [<хэш>] - установка метки на указанный (или на последний) коммит
  git tag -a <имя метки> [-m "<описание>"] [<хэш>] - установка аннотирующей метки на указанный (или на последний) коммит
