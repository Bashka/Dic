ВОЗВРАТ
  git checkout <хэш> [<имя файла>] - возвращает систему (или файл) в состояние, определенное указанным коммитом
  git checkout -- [<имя файла>] - возвращает систему (или файла) в состояние, определенное предыдущим коммитом
  git checkout <метка> [<имя файла>] - возвращает систему (или файл) в состояние, определенное указанной меткой
