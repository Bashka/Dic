IF
  if ...; then ...; fi - конструкция if

CASE
  case слово in шаблон) команды ;; esac - выполнение команд, для которых слово соответствует шаблону

ПЕРЕНАПРАВЛЕНИЕ
  if test -z $a; then echo 'empty'; else echo 'full'; fi > file - перенаправление всего вывода if в файл

ОПЕРАТОРЫ СРАВНЕНИЯ
  = != > >= < <= - сравнение строк
  -n строка - строка не пуста
  -z строка - строка пуста
  -eq/-ne - сравнение чисел (равно, не равно)
  -lt/-le - сравнение чисел (меньше, меньше или равно)
  -gt/-ge - сравнение чисел (больше, больше или равно)
  -e файл - файл существует
  -d файл - файл существует и является каталогом
  -f файл - файл существует и является файлом
  -r/-w файл - есть право на чтение/запись в файл
  -s файл - файл существует и он пуст
  файл -nt файл - первый файл новее второго
  файл -ot файл - первый файл старше второго
