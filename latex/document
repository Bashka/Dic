ПРЕАМБУЛА
  \documentclass{класс} - определение класса документа
  \usepackage[параметры]{имя} - подключение сторонних пакетов
  \includeonly{файлы} - список файлов (через запятую), которые необходимо подключить с помощью include

ДОКУМЕНТ
  \begin{document} - начало тела документа
  \end{document} - конец тела документа
  \input{файл} - подключение файла в тело другого файла
  \endinput - метка, определяющая границу подключаемого файла
  \include{файл} - подключение файла (без расширения .tex) в тело другого файла с новой страницы
