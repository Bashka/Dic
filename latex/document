СПЕЦСИМВОЛЫ И КОМПОНЕНТЫ ЯЗЫКА
  { } $ & # % _ ^ ~ \ - спецсимволы, используемые транслятором. Для их экранирования необходимо добавить символ \ перед ними

  %...\n - комментарий

  $...$ - математическая формула
  _ ^ - нижний и верхний индексы

  ~ - неразрывный пробел
  \ ... - принудительный пробел

  \неБуква - начало односимвольной, неименованной команды TeX (пробел после команды не обязателен и учитывается на печати)
  \команда ... - начало именованной команды TeX (пробел после команды (или не буквенный символ) обязателен и не учитывается на печати)
  \команда{a,b,c} - команда с обязательными аргументами
  \команда[a,b,c] - команда с необязательными аргументами

  \параметр=значение - установка значения параметру

  {...} - группа
  {\команда ...} - команда, работающая только внутри блока и отключающаяся автоматически по выходу из него

  \begin{окружение} ... \end{окружение} - объявление окружения (многоабзацной группы). Окружения имеют те же свойства, что и группы

ПРЕАМБУЛА
  \documentclass{класс} - определение класса документа (book - книга, article - статья, report - отчет, proc - конференции, letter - деловые письма)
  \usepackage{имя,имя,...} - подключение сторонних пакетов
  \includeonly{файлы} - список файлов (через запятую), которые необходимо подключить с помощью include

ДОКУМЕНТ
  \begin{document} - начало тела документа
  \end{document} - конец тела документа
  \input{файл} - подключение файла в тело другого файла
  \endinput - метка, определяющая границу подключаемого файла
  \include{файл} - подключение файла (без расширения .tex) в тело другого файла с новой страницы
