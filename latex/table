ПЛАВАЮЩИЕ ТАБЛИЦЫ
  \begin{table} ... \end{table} - определение таблицы, которая должна располагаться в окружении, как плавающей. Таблица автоматически будет перенесена на такую страницу, где она пометиться целиком
  \caption{описание} - подпись таблицы

ОБТЕКАЕМЫЕ ТАБЛИЦЫ
  \begin{wrapfigure}{положение,ширина} ... \end{wrapfigure} - определение обтекаемой иллюстрации. В качестве аргумента Положение указывается: l - слева, r - справа, o - снаруже, i - внутри. В качестве аргумента Ширина указывается ширина изображения
  \begin{wraptable}{положение,ширина} ... \end{wraptable} - определение обтекаемой таблицы. В качестве аргумента Положение указывается: l - слева, r - справа, o - снаруже, i - внутри. В качестве аргумента Ширина указывается ширина таблицы (необходим пакет wrapfig)

СПИСКИ
 \listoftables - генерация списков таблиц

ПСЕВДОТАБЛИЦЫ
  \begin{tabbing} ... \end{tabbing} - окружение псевдотаблицы (табуляции)
  ...\=...\= - определение позиций табуляции
  ...\>...\> - переход между табуляциями
  \pushtabs - новый массив табуляций
  \poptabs - возврат к предыдущему массиву табуляций
  ...\>...\'\\ - прижать последнюю табуляцию к правому краю
  \+ - начинать новую строку с n+1 табуляции
  \- - начинать новую строку с n-1 табуляции

ТАБЛИЦЫ
  \begin{tabular}{поля} ... \end{tabular} - окружение таблицы. Поля задаются символами: l - содержимое выравнивается по левому краю, c - по центру, r - по правому краю, p{ширина} - по ширине с переносами, | - вертикальная разделительная черта
  ... & ... - разделитель полей таблицы
  ... \\ ... - разделитель строк таблицы
  \hline - горизонтальная разделительная черта
  \multicolumn{числоПолей}{выравнивание}{содержимое} - объединение полей таблицы. В качестве аргумента Выравнивание указывается l,r,c или p{ширина}

МНОГОСТРАНИЧНЫЕ ТАБЛИЦЫ
  \begin{longtable}[выравниваниеТаблицы] ... \end{longtable} - окружение многостраничной таблицы. В качестве необязательного аргумента задается l - выравнить таблицу по левому краю, c - по центру, r - по правому краю
