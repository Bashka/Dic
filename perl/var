ТИПИЗАЦИЯ
  $... - значение ('строка', 1)
  @... - список ((элемент, элемент, элемент))
  %... - ассоциативный массив (хэш таблица)
  &... - функция

СКАЛЯРНЫЕ ПЕРЕМЕННЫЕ
  $имя = значение - инициализация переменной типа строка/число
  ++--$имя++-- - префиксная и постфиксная формы инкремента и декремента
  my $имя = значение; - объявление и инициализация переменной с локальной областью видимости

ЧИСЛА
  1 - целое число
  + - * / - операции над числами

СТРОКИ
  'строка' - строковая константа
  "строка" - раскрываемая строка
  'a' . 'b' - конкатенация строк

  split(regexРазделитель, строка) - разбить строку на массив по разделителю
  uc(строка) - приведение всех букв к нижнему регистру

МАССИВЫ
  @массив = ('a', 'b', 'c'); - инициализация массива
  $массив[индекс] = значение - установка значения элементу массива
  $массив[индекс], ${массив[индекс]} - получение элемента массива
  $#массив - индекс последнего элемента массива
  $#массив + 1 - число элементов в массиве
  @массив[0] - получить массив из одного элемента, содержащего значение элемента под индексом 0
  @массив[0,2] - получить срез массива из трех элементов от 0 до 2
  ($var, $var, $var) = @массив - заполнение переменных значениями массива

  join(разделитель, массив); - объединить массив в строку с заданым разделителем

ХЭШИ
  $хэш{ключ} = значение - установка значения элементу хэша
  $хэш{ключ} - получение значения хэша
  
  reverse(хэш) - перестановка ключа на место значения и значения на место ключа и возврат результирующего хэша

ССЫЛКИ
  \@массив - ссылка на массив (скалярное значение)
  \%хэш - ссылка на хэш (скалярное значение)
  $ссылка->[индекс] - получение элемента массива через ссылку
  $ссылка->{индекс} - получение элемента хэша через ссылку
  [функцияВозвращающаяМассив] - получение ссылки на массив, возвращаемый функцией
  {функцияВозвращающаяХэш} - получение ссылки на массив, возвращаемый функцией

АРГУМЕНТЫ СКРИПТА
  @ARGV - массив аргументов скрипта
