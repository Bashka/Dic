ОБЪЯВЛЕНИЕ СКРИПТА
  #!/usr/bin/perl - первая строка в файле Perl скрипта, определяющая адрес интерпретатора

ПОСТРОЧНАЯ ОБРАБОТКА ВХОДНОГО ПОТОКА
  perl -pe 'regex' - циклический проход по STDIN и вычисление regex. Результат выводится в STDOUT
