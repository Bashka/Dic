IF
  if(условие){тело}elsif(){тело}else{тело} - оператор ветвления

?:
  (условие)? значениеИстина : значениеЛожно; - тернарный логический оператор
ЛОГИЧЕСКИЕ ОПЕРАТОРЫ
  == != > >= < <= - операторы сравнения чисел
  eq ne lt le gt ge - операторы сравнения строк (аналогично предыдущим)
  строка =~ m/шаблон/ - определение соответствия строки регулярному выражению
  && || ! - операторы И ИЛИ НЕ
